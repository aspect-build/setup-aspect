{
  "name": "setup-aspect",
  "version": "0.0.0",
  "description": "Install the Aspect CLI launcher and configure Bazel for GitHub Actions",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "ncc build index.js -s -o dist/main && ncc build post.js -s -o dist/post"
  },
  "author": "Aspect Build",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/aspect-build/setup-aspect.git"
  },
  "dependencies": {
    "@actions/cache": "^6.0.0",
    "@actions/core": "^3.0.0",
    "@actions/exec": "^2.0.0",
    "@actions/github": "^9.0.0",
    "@actions/glob": "^0.6.0",
    "@actions/io": "^3.0.0",
    "@actions/tool-cache": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
